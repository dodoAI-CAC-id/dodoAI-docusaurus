# Goal
* Add a new feature of "Blocking User"

# instruction
* We have to implement Frontend interface of communiticating with "Blocking User" API.
* We already have a API defition for "Blocking User". The API documents are located as following.
    - CREATE Block: xxx/xxx/xxx.md
    - PUT Block: xxx/xxx/xxx.md
    - GET Block: xxx/xxx/xxx.md
* User can excute Blocking feature from user profile page, adding a button to show "Block User" button.

# Target Folders
 AI Pipeline
  * `docs/docs/ai-pipeline/specification/pipelines_list`
* Action
  * `docs/docs/ai-pipeline/specification/action_lists`

# Development Rules
* Be sure to update /plan. For this task, create a folder named Issue-xxx.
* All documentation must be written in **English** under the `docusaurus` directory.
* When updating Docusaurus, always update `/sidebars/` as well.
* Documentation must be written in **English**!!
* Docusaurus markdown must include `id` and `title`.
* Use **Mermaid** for diagrams when necessary.
* KEEP TDD(test driven development)!!


# Detailed Design Structure
* Strictly follow TDD

# Restrictions
* Do not cause Docusaurus MDX errors or Mermaid syntax errors.
* Do not build Docusaurus unless explicitly instructed.

